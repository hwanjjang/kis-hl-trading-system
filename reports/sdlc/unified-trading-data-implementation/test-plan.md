# Verification plan
AC1/AC2: additive migrations preserve operating rows, immutable raw bytes, repeated observations, revision/as-of selection and exact Decimal validation; reject auth payloads. AC3: fill/cost conservation, overlap funding quarantine, KIS DAY precision, account/currency separation and reproducible reports. AC4: weekly ten-year boundaries, missing sessions, provider variants, partial bars and snapshots. AC5: manifest digests/roles, repeat import, online backup and isolated restore, preview-only retention; bounded market writes. AC6: real CLI smoke through import, repeat import, market fixture collection, journal report/export, pinned analysis and backup/restore.

Unit tests use synthetic data and stubbed transports; no network or orders. The additional smoke runs real CLI subprocesses with a temporary SQLite database, validates resulting rows and exports and removes its temporary directory. Failures include changed duplicate totals, mixed currency sums, falsely complete weeks, missing input provenance and a restored database failing integrity. Private source rehearsal retains receipts under ignored data only. Existing regression suite must stay green. Independent verification has separate context and report.
