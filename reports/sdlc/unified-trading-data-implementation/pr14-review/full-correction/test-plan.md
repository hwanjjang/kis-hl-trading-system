# Full review correction: acceptance and verification

Scope: all twelve distinct mandatory issues accepted from Claude/Fable M1-M6 and Codex C1-C8 (M2=C2; M3=C4). Include scoped journal reads, read-only status/schema discovery, incremental account/history-audit jobs, and metric exclusion visibility. Preserve prior review evidence and unrelated dirty work. Endpoint: update existing PR #14; no merge/live calls.

Builder: /root (OpenAI), same task unified-trading-data-implementation. Test-design iteration 3; build remaining allowance checked before entry. Existing supplementary PR review attempts total 10; do not reset or launch iteration 11. Complete authorized implementation/verification and report the review-stage limit honestly.

## Acceptance

- AC2/AC3: statement reconciliation rejects contradictions, positive overseas quantities, same-observation identity ambiguity rejected with raw evidence retained; ordinary validated daily/cumulative maturation replaces prior economics; ambiguous domestic fee groups stay pending without blocking other markets.
- AC3: recover HL cycles only at explicit source-backed flat anchors, retain prior invalid segment fees; bounded statement-backed reconciliation enables coverage/position evidence without claiming API lifetime completeness; scoped SQL reads and metric exclusion counts.
- AC2/AC4: late facts/coverage changes invalidate current projection freshness, old exports remain immutable; current analysis rejects obsolete derived inputs and preserves historical-as-of behavior.
- AC4: correct KIS book response blocks, index basis, overseas minute paging/timezone; explicitly reject testnet market collection before capture to avoid network collisions. Existing unknown market provenance is not relabeled.
- AC1/AC5/AC6: read-only status/migration preview, checked schema discovery, bounded recurring overlap and separately configured history audits, visible worker heartbeat. Real temporary SQLite/CLI smoke verifies imported evidence, reconciliation, journal/export, stale detection and non-mutating previews.

## Tests and separate smoke

Add behavior-focused unittest regressions before each owned code change and retain intended Red, Green, post-refactor evidence. Run existing full suite after integration. Separate smoke uses fresh temporary SQLite and real CLI processes, disables credentials/network, exercises reject/import/reconcile/report/export/status, verifies outcomes and cleanup. Public KIS route contract checked against official sample; transport tests stub network. No account or trading API requests.

Existing architecture retains raw observation -> validated revision -> quality/coverage -> journal/analysis projection. Corrections implement existing contracts rather than introduce a service or store; existing Archify structural artifact remains applicable. New bounded reconciliation is a local validation step in that existing path, documented with explicit evidence trust and failure semantics.
