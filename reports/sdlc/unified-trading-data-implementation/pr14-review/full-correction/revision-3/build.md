# Full correction build iteration 7

Replaces the watermark-dependent freshness predicate with comparison of current effective scoped trade/cash fact IDs against pinned inputs. Historical as-of and both concurrent append boundaries now expose omitted historical facts. Old superseded revisions are excluded, so a fresh report is not falsely stale. Coverage is compared against captured coverage IDs. This removes journal max-ID reads and unused pin watermark fields, while supporting prior reports that lack them. Two new regressions cover historical known-time selection and already-corrected fresh reports; the first failed before implementation. The independent interleaving probe now returns stale_runs=[1]. All prior accepted review corrections remain in scope; prior failure evidence retained. No schema or live behavior changes.
