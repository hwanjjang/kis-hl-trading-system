# G1 test and smoke plan
AC2/AC4: tests/test_statement_cost_quality.py covers mixed known/null costs above/below total, explicit signed rebates, zero/all-known controls, buy/sell, old direct facts, cycle/statistics/account totals, independent reconciliation on both evidence sets and correction without old-export mutation.
AC5/AC6: backup tests reject missing and uninitialized sources without creating files, preserving working online backups. Documentation review confirms lock rejection, KIS live-only limitation and partial implementation status; no provider call changes.
Required additional smoke: scripts/smoke_statement_costs.py runs real CLI subprocesses against a TemporaryDirectory SQLite store. Import unresolved statement, reject certification, generate pending journal, export, correct source, certify, regenerate finalized net returns, verify old export, backup and isolated restore. Also fail missing-source backup without files. Dotenv disabled, sanitized environment, no network. Any unexpected finalization, cash-total confirmation, artifact mutation, command result or remaining temp directory fails. Prerequisite: installed Python dependencies. Cleanup: TemporaryDirectory and explicit absence assertion.
Red must fail intended new assertions on old production code, then Green full suite and focused post-refactor checks on final candidate, followed by additional smoke and independent verification. Existing market/legacy tests prove unaffected contracts; no artificial live API test.
