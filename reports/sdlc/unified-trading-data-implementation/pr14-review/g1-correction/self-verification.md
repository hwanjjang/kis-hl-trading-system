# G1 self-verification
Candidate: candidate.json; all listed files rehashed and unchanged. AC2/AC4: mixed fees are retained pending; exact signed/equal known totals remain eligible; historical facts, both reconciliation populations, account isolation, statistics and immutable correction exports covered. AC5/AC6: missing/uninitialized backup sources reject without initialization; real online backup/restore returns unchanged economics. AC1/AC3: unaffected storage/market contracts covered by full regression suite.
360 full tests passed; 82 focused post-refactor tests passed. Separate offline runtime checks: 15 new and 12 existing real CLI subprocesses passed, each with temporary SQLite, sanitized environment, dotenv disabled and cleanup assertions. Green source/test/docs bytes match the final candidate; the standalone smoke script was added afterwards and is not loaded by unittest, so the green tests remain applicable. That script was executed separately on its final bytes. No product refactor after Green. git diff --check passed. Documentation links resolve; existing architecture diagrams reused because no structural change. Original failed fixture setup is preserved separately and is not counted as intended Red.
No provider network, actual account data or secrets accessed. Formal supplementary PR review remains at its prior ten-attempt limit; this is bounded correction acceptance, not merge readiness. Other recommendations (grain validation, position precision, freshness noise and structural cleanup) remain deferred.
