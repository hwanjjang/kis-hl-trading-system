# G1 build
Candidate/file hashes: candidate.json; actual patch: changes.diff. Base 294e517.
Added one shared cost-quality function and reused it for parser contradictions, every journal read and both independent reconciliation populations. Mixed-null mismatches remain source facts but cannot finalize returns. Explicit known signed rebates remain supported. All totals/components remain separately visible; no guessed costs, new schema, order calls or data rewrite. Backup reads existing schema through readonly DataStore. Updated journal contract and operation/design status docs.
Regression-first evidence: red-setup-error.txt preserves a fixture missing its required observation argument; fixed the fixture before production edits. red.txt is the intended Red: 10 tests, 16 assertions failing for mixed cost confirmation and backup side effects, no setup errors. Green full suite: 360 tests. Additional focused and CLI evidence follows in self-verification. No broad refactor was necessary.
