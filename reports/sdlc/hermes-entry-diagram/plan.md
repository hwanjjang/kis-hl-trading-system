# Documentation plan

Owner: /root. Selected notebook: .planning/2026-09-23-hermes-entry-diagram.

1. Create and check the Archify sequence source and HTML (design worker owns the two artifacts and design evidence). Show current managed Hyperliquid new entry, with optional signal authority as a secondary note.
2. Add the operations walkthrough and discovery links in README and architecture; clarify that concurrent local backup starts with observed fills, not only after native activation. Main owns prose. Preserve existing manual handoff and target views.
3. Freeze the exact documentation candidate. Check relative links/anchors, CLI help syntax without execution, changed-file scope, and Archify artifact/browser/visual evidence.
4. A fresh non-builder independently challenges the diagram and prose against repository code. Address mandatory discrepancies before local delivery.

AC1 maps to source and CLI command checks; AC2 to supervisor/gateway semantic verification; AC3 to link checks, nine Archify artifact checks, four desktop browser sizes and screenshot review. No runtime behavior changes: runtime TDD/smoke are not applicable; do not rerun the feature suite under a documentation smoke label.

Repository instructions, installed skill identities and exposed host capabilities are recorded in host-and-dependencies.json. No live venue access, publication or integration occurs in this task.
