# Spec

AC1: show Hermes -> CLI -> durable SQLite queue -> account supervisor -> Hyperliquid; commands prepare/preview/submit; submit is not a fill. Explicitly show --live on producer and consumer, separate worker lifetime, same DB/account. AC2: show partial fills with fixed SL; terminal entry plus verified fixed SL enables one native submission; native mark tracking and concurrent local nine-minute backup; verified readback and residual reduce-only exits through flat cleanup. State worker dependency and unverified live exchange contract. AC3: fresh Archify sequence JSON and HTML under docs/architecture/hermes-entry.*, linked from README, operations and architecture; English, nine artifact checks and real browser/visual evidence. Optional signal/grant mode remains distinct, no claim of built-in Hermes SDK or strategy evaluation/notifications. No executable source edits.
