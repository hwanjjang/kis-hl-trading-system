# Build

Added the dedicated Hermes new-entry sequence JSON/HTML, operations walkthrough and discovery links. Corrected architecture wording so local monitoring may start on fills before native trailing submission. Runtime files unchanged. New diagram files are bound directly through candidate-source hashes; changes.diff covers existing prose. Candidate file set and base are recorded in candidate.json.
