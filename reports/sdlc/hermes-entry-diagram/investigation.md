# Investigation

Inspected operations_cli.py, managed_execution.py, managed_gateways.py and strategy_signals.py plus docs/trading-operations.md and docs/architecture.md. order prepare obtains execution ATR; preview validates plan; submit queues QUEUED with no broker write. Worker consumes matching account/mode under process lock. Paper reaches PREVIEWED; live needs submit and worker --live. Worker preflight checks quote, ATR, ownership, funds/exposure and authority, freezes native distance, persists attempts then transmits. Actual partial fills get fixed SL. Local backup can start before native activation. Native submits once after entry terminality and verified full fixed-SL coverage. Waiting differs from active readback. Reduce-only exits reconcile residuals; flat cleanup retains protection until appropriate terminal evidence. Signal ingestion alone grants no execution authority; signal execute requires manual or bounded grant and shares the queue. Existing older diagrams include future components; manual-position-handoff covers existing positions only.
