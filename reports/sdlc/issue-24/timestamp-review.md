# PR 26 main integration and candle-time improvement

Reviewed latest local merge 6d9cd13 (main a156b69) against PR26's strategy work. No required integration fixes found: configured subaccount scope is shared by grant creation, signal execution and supervisor; old master-scoped grants cannot authorize subaccount entries. Advisory sizing remains account-local. The timestamp follow-up adds start_ms/end_ms as fallback aliases after existing legacy names in the shared BTC helper. Canonical output times and chronological current/reference selection are preserved, including reverse-order direct helper inputs. The strategy wrapper still rejects unordered or incomplete snapshots before calling the helper.

Regression first failed for reverse canonical input. Six signal tests passed after the two-line fix. With declared dependencies installed in isolated .venv, 133 relevant tests passed; initial system-Python run had 11 import errors from missing SDK packages, resolved without source changes. Python 3.11.15, hyperliquid-python-sdk 0.24.0, eth-account 0.13.7. Separate CLI subprocess smoke: ordered strategy evaluation accepted, unordered rejected; BTC breakout CLI with stubbed candle transport returned all four expected timestamps. No live API or order calls.

Independent reports separately cover integration (21 focused tests and account-scope probe) and timestamp change (16 tests plus helper/wrapper probes), both PASS. No structural change/diagram needed. Code change is two fallback keys plus two regression tests and the owning tool documentation. This resolves the prior optional timestamp recommendation; no need for a larger adapter or runtime change.
