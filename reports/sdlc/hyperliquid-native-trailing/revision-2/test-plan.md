# Corrective verification plan
AC2: distance normalization honors decimal and own significant-figure precision, persists before entry, zero rejects with no entry; real gateway returns raw decimal tick; parser supports known syntax while managed unit/activation mismatch stays rejected.
AC3: rejection/unknown never resends/exits solely for failure; fixed stop loss after this intervention still exits; unrelated reconciliation failures freeze. Waiting across grace and restart retains full fixed stop, reports zero trailing coverage, transitions to active only on verified readback; fixed SL loss and accepted cancellation still exit. Flat cleanup includes known trailing and stop orders.
AC4: Red on original runtime -> focused Green -> full regression -> separate CLI/SQLite/actual SDK smoke plus real gateway/supervisor replay with stubbed exchange boundary. Unique TMPDIR per actor prevents fixture lock interference. No network or funded account. Temporary DB/key cleaned on process exit. Functional failure is any duplicate trail, wrong state/coverage, nonzero entry for rejected precision, wrong signing recovery, or unexpected exit during waiting/rejection. Exact commands/results retained in revision-2.

Additional required regression: accepted known-ID trail active then rejected must exit residual; only no-oid submission rejection follows native intervention. Existing diagram step 6 is submission response, step 10 is accepted terminal; unchanged diagram evidence remains applicable.
