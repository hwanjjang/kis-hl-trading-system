# Corrective build
Builder /root, smoke contribution /root/correction_smoke; runtime candidate in candidate.json. Native rejection/no-oid handling now preserves fixed-SL monitoring, while unrelated reconciliation errors clear the native-specific intervention exception. Waiting with matching full-sized open readback is not active coverage and does not cause a timeout exit. Distance normalization shares the client price increment contract, is persisted before entry, and uses the metadata decimal tick separately from the quote grid. Parser recognizes known clauses, then verifies quote/immediate semantics.
Files: trailing.py (precision/parser), client.py (shared validation), managed_gateways.py (raw tick), managed_execution.py (preflight/transitions), tests/test_native_trailing.py (14 added regressions), smoke_native_trailing.py (real gateway replay), operations/architecture/API reference docs.
Red: 27 tests, 5 failures and 2 missing-helper errors before runtime edits (intended expectations). Initial Green 27 passed. Extended Green 104 passed. A repeated-fixture snapshot test initially failed because gateway reconciliation mutates the fixture order ID; use independent fixture per scenario, without changing product behavior. Full regression 447 passed. No refactor after Green. Additional smoke passed with zero network calls. No live acceptance claim.

## Renewed build, iteration 3
The initial corrective candidate was blocked by a known-ID accepted trail later becoming rejected. Its rejection status incorrectly matched the never-accepted exception. Restrict that exception to missing order_id; acknowledged terminal orders retain residual exit. A targeted regression failed before this edit and passes afterward. Full corrected evidence: 105 focused tests, 448 full regression, and separate integrated smoke all pass. Candidate now sha256:e5ac77a4207f4964f7146a84838d74182b276b1afed0211a4b7779c0959391a9. Earlier counts above are historical, not final attestation. No design geometry change: existing submission-response versus accepted-terminal diagram branches already express the intended distinction.
