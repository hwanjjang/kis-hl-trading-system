# Investigation
Current validate_plan defaults local; persisted supervisor reads missing selector as local. Therefore change only new-plan normalization and make local-oriented test fixtures explicit. Legacy trailing enroll supports local only and demands existing entry/SL; do not silently route native into it.
Managed gateway snapshots derive net fills from row.created_ms and owned entry attempt IDs. Adoption needs fill_history_start_ms from original entry, actual first_fill_ms, and separate current created/adopted time. Supervisor daemon holds account lock across its loop: CLI must queue ADOPTING, with validation/import performed by the supervisor under its lock. Atomic position+attempt binding prevents half-owned state. A failed/pending adoption must never enter QUEUED entry recovery. Imported native IDs must not be rebound after completion or conflict with a legacy manager. Journal attribute_fill currently labels matched attempts agent; skip imported orders to preserve source provenance.
Reads reuse known metadata/orderStatus/fill/open-order/state APIs; no new signed action. Existing fixed SL is a prerequisite. Missing SL requires a separate explicit protective order under the selected risk policy, not hidden behavior in adoption. Continuous native trail uses existing once-only machinery; local alternate reuses the existing nine-minute worker policy. No automatic local takeover of UNKNOWN native submissions.

Concurrent backup can reuse the existing Trail.tick and exit intent rather than creating a second worker or fallback switch. Keep fixed-SL coverage anchored to entry ATR, while local threshold may ratchet independently. Native orders remain until flat; native fills and local IOC executions are reconciled through the same account ledger and reduce-only invariant. Existing native tests plus new local-trigger/native-pending/restart races must cover this.
