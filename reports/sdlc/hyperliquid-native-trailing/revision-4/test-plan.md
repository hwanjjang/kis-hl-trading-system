# Native default/manual handoff test plan
AC1/AC2: HL new native+local backup normalization, KIS local, explicit choices, dry-run default and historical compatibility. AC3: single coordinated exit with native waiting/error, partial/flat reconciliation, no duplicate trail; queued adoption from actual entry+SL, complete flat-to-long ledger, VWAP/size/stop/retention validation, legacy/duplicate ownership, transaction rollback, recovery never entry, imported fill origin preserved. AC4: docs/diagram and independent verification.
Red command: TMPDIR=/tmp/hl-pr21-r4-builder python3 -m unittest tests.test_manual_adoption -q; focused Green includes native_trailing, managed_execution, managed_gateways, operations_cli and journal_history. Full post-refactor: python3 -m unittest discover -s tests -t . -q.
Required additional smoke scenario offline-functional: dependencies in /tmp/hl-trailing-venv; PYTHONPATH=. TMPDIR=/tmp/hl-pr21-r4-smoke /tmp/hl-trailing-venv/bin/python scripts/smoke_manual_handoff.py. Actual CLI+SQLite queued handoff, gateway+supervisor original fill history import, native once, concurrent local reduce-only exit, partial native fill race, restart and terminal cleanup; simulated exchange boundary only. Also rerun existing SDK/CLI native smoke. Success: no new entry/import atomic, backup exits and reconciles residual, retained SL/trail until flat; no network or real credentials. Failure: any duplicate action, wrong ownership/mode, lost exit, false closure. Cleanup: temporary database files and ephemeral signer end with process.
