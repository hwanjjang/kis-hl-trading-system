# Plan

Implement tests first, fix PR17 then test and commit; merge it into PR18 and fix order-specific behavior. Run full suite with isolated lock directory and separate loopback/SQLite smoke. Update canonical API skill and diagrams; defer large managed-trading/canonical-data redesign with reasons. No production calls. Revert commits to roll back; no destructive schema change.
