# Independent documentation audit — Binance PR review corrections

Auditor: independent Codex sub-agent. Read-only source inspection. Only this report written.
Reviewed baseline PR17 `b149a52876a0c6568773dbf3e6fce118041c87fd` and PR18 `7e13326baa8815feec75992f4a977eb137623aa2` using `git show`, alongside the full review report. No signed/live requests, remote publication, or source edits. Vendor claims below are inherited from repository evidence, not independently revalidated over the network; factual endpoint/code edits should use current official documentation during implementation.

## Must Fix: minimum exact changes

| Finding | Where | Minimal correction |
|---|---|---|
| F15 | PR18 `.agents/skills/binance-api/references/rest-endpoints.md`, heading `Order endpoints (NOT implemented...)`; public/signed tables | Replace obsolete order paragraph with a short routing index linked to `orders.md`: regular MARKET/LIMIT `/order`, conditional `/algoOrder`, corresponding GET/DELETE. Add wrapped `/ticker/price`, `/positionSide/dual`, `/openAlgoOrders`, `/algoOrder` reads. Keep modify/leverage/margin-type explicitly unimplemented. Do not duplicate the full order parameter table. |
| F15 | PR18 `limits-and-errors.md`, `SKILL.md` §4/§5, `websocket.md` Other events | Remove future-iteration label. Distinguish vendor response semantics from the implementation's conservative classification; link order retry/outcome rules to `orders.md`. Correct `activationPrice` to `activatePrice` in the Algo API context. Add CANCELED to the documented algo lifecycle after checking official contract. |
| F16 | PR18 `docs/architecture.md`, Open Risks (currently line 159) | Replace verified-exchange-test claim with: "Binance order placement has unit-test and dry-run smoke evidence only. The signed regular-order test request returned -2015; successful exchange validation and demo/live fills remain unverified. Algo orders have no test endpoint, and their response/status shapes have not been observed in this environment." This records evidence without claiming a fill or authorizing one. |
| F29 | PR18 `SKILL.md:27-28` | Replace "strongest smoke an agent may run. Never run --live" with neutral endpoint behavior plus "This skill grants no execution authority; follow the shared agent rules and applicable session instructions." Link AGENTS.md and Claude-specific section only for Claude; do not create new shared rules in CLAUDE.md. |
| F43 | PR18 `README.md:439`, `docs/architecture.md` trading component and Safety Decisions, `SKILL.md:100-103,144-146` | Put one outcome contract in `references/orders.md`; link other files to it. README should say unresolved outcomes need manual investigation before any retry; local unknown protection rows are not automatically reactivated. The current "never as rejected" is false after confirmed terminal/no-fill reconciliation. Avoid suggesting any retry is safe merely because it reuses an ID. |
| F44 | PR18 `docs/architecture.md` Data Flow index; `binance-trading-data-flow.json` views/trade note, guard node, edge label, boundary card; matching HTML; `.env.example:38-39` | Remove planned from implemented guard/trading client/round-trip; retain planned for strategy automation. Regenerate HTML from JSON with archify. Add "Live orders require futures trading permission" to env comments. Read-only permissions remain sufficient for read-only commands, so retain that distinction. |
| F45 | PR17 `docs/architecture.md`, final Binance Open Risks route paragraph | Replace copied tier dates, migration history and legacy-root behavior with implementation consequence: "A market stream connection handles one route tier; run separate commands for mixed tiers. See [Binance WebSocket contract](../.agents/skills/binance-api/references/websocket.md)." The component summary and README operator usage belong where they are; wholesale deletion is unnecessary. Update stale listenKey text with F01, and link lifecycle details rather than repeating dates/cadence everywhere. |

F18 has already been resolved by the main merge; avoid presenting it as newly fixed code.

## Recommended documentation corrections worth including now

These are small corrections to files already touched by the Must Fix work; none needs architectural expansion.

- **F04** (`SKILL.md:103`, `README.md:439`, `orders.md` outcome follow-up): say "Do not resubmit until exchange state is resolved; reusing a client ID is not durable idempotency." Current "with a new ID" restriction can imply same-ID retry is safe. Preserve no automatic retry behavior.
- **F06** (`docs/trading-operations.md`, README Binance intro): explicitly describe manual Binance commands as outside managed plans, risk caps, pause-entries, supervisor, account/journal integration. A stored protective row is not independent coverage verification. Link README to this operational boundary. Do not repeat refuted F05 as if only entry is disabled: the current empty allowlist really disables all current Binance live commands.
- **F27** (`README.md:441`, architecture Binance client/WS summaries, skill §3/§6, `websocket.md:37`): change "fresh"/"mints" to "requests a listenKey on each connection (the exchange may return the current key)". The client exposes close, but the runner never invokes it. Split wrapper availability from automatic lifecycle behavior; do not add DELETE-on-disconnect without considering shared account keys.
- **F32** (`README.md:282`): omit optional `--activation-price 74000` from the trailing example. Fixed activation prices age into wrong-side values. Explain optional direction in the owner order reference. The heading "always fails" was time-dependent, not universally true.
- **F33** (`SKILL.md:17-19`, Environments, `.env.example:38-45`): list demo profile and dedicated DEMO credentials, with automatic demo environment selection. Keep README as setup owner and link from skill if reducing duplication.
- **F34** (`SKILL.md:146`): remove false assertion that -1008 is inherently 4xx. Clearly label vendor overload rejection versus implementation's conservative 5xx handling; do not silently change runtime classification as a doc fix.
- **F35** (`limits-and-errors.md` quantity codes, `orders.md` rejections): correct -4003 negative quantity, -4004 below minimum, -4005 above maximum, -4014 tick mismatch, -4023 step mismatch. Keep one error-code table and link other reference. Do not change negative to <=0: the review suggested fix overstates the named vendor code.
- **F36 documentation part** (`rest-endpoints.md` exchangeInfo Params): endpoint returns full universe; symbol selection is local. Caching is separate behavior.
- **F39 documentation part** (architecture storage paragraph, skill storage): accurately distinguish raw KIS/vendor responses from current normalized Binance tick payloads unless code is changed to retain the original frame.
- **F42** (`SKILL.md:171-174`): route is method+path; use explicit HTTP method for any authorized unauthenticated probe. Do not call `/algoOpenOrders` nonexistent: DELETE uses that route. Probe status is supporting evidence, not API-contract proof or a blanket permission to probe mutations.
- **F58** (`orders.md:18,48-51,64-68`): move regular RESULT response under regular orders; include terminal states handled by code; do not claim observed `/order/test` response shape. Account for -1000/-1006 unknown-execution messages before generic 4xx row.
- **F60** (`binance-order-roundtrip-sequence.json` and generated HTML): "no network" -> "no signed call"; store arrow originates at CLI, and dry-run persists too. Show one-way-mode read and unknown lookup if regenerating layout. Reconciliation can lead to submitted/rejected/unknown, not always unknown.
- **F61** (architecture Data Flow provenance): split legacy diagrams from Binance diagrams, rather than attaching old `e012d...` revision to all diagrams. Include Binance in high-level prose/Mermaid where it describes the full current system.
- **F62** (`AGENTS.md` Documentation Binance bullet): moving shared no-vendor-trading-MCP sentence to Trading Safety preserves behavior and clarifies ownership; optional cosmetic scope.
- **C03** (`CLAUDE.md` ownership row and binance sync sentence; skill description): explicitly mention trading.py, order/algo/cancel contract. Existing broad AGENTS rule already covers it; do not claim policy currently lacks any coverage.

## Recommendations that can be deferred with explicit boundaries

Deferral here is scope separation, not waiting for unspecified evidence. Record IDs, residual behavior, and a concrete next acceptance criterion; do not mark them fixed.

| Findings | Why separate work is reasonable | Required next scope / present mitigation |
|---|---|---|
| F11 | Adding environment/account identity to existing historical tick/order data requires schema/backfill/CLI compatibility policy; key profile alone is not stable account identity. | Scoped migration with legacy unknown scope and isolation tests. Until then document/use distinct DB paths per environment/profile. |
| F13, F14 | Buffered ingestion and canonical unified-data integration change persistence guarantees, shutdown/backpressure, and consumer contracts. | Bounded writer/replay design with sustained-rate tests and crash semantics. Current synchronous lag remains a limitation; do not claim production high-frequency capture reliability. |
| F03, F20, F21 | Durable pre-send attempts, request-identity reconciliation and protection recovery form one lifecycle feature; isolated patches can create misleading coverage. | Persist intent before signed send; scoped unique IDs; exchange identity/shape validation; recover interrupted/unknown attempts without resubmission. Keep unknown rows inactive and clearly document manual investigation. |
| F28 and F06 managed integration | True account identity and whole-operation ownership cannot be fixed by extending an API-key lock's lexical block alone; shared supervisor gateway integration is beyond raw CLI changes. | Stable account identity + ManagedBinanceGateway/instrument integration, ownership/reconciliation tests. Document raw CLI boundary now. |
| F36 cache portion | Persistent filter cache needs an explicit freshness and invalidation contract for money-moving guards. A quick permanent cache could make validation less safe. | Freshness-bounded cache and stale/refresh-rejection tests; fix false endpoint parameter docs now. |
| F41 | A >90-day algo cancel policy needs account-scoped identity evidence or a verified alternative listing, not simply skipping symbol verification. | Verify venue retention alternatives; preserve current fail-closed behavior and document the retention limitation. |
| C02 | TRIGGERED currently denotes an algo that may have an outstanding child order. Removing it from active without child-order reconciliation can misstate protection in the opposite direction. | Define coverage separate from algo lifecycle, reconcile child order/fills and remaining quantity. Do not claim active row alone establishes coverage. |
| F19 | Restoring/deleting unrelated historical diagram receipts conflicts with explicit earlier user preference to remove screenshot artifacts and is unrelated to runtime fixes. | Clarify JSON/HTML receipt policy separately; do not restore all generated artifacts as a review workaround. |

C01 deserves special treatment: a live wrong-side protective stop is an actionable correctness concern even though tagged Recommended. Fix explicit position/coverage validation if the order path is being changed, or prominently preserve the limitation; do not label it cosmetic. F07, F10, F17, F24, F25, F26, F30/F31, F46/F47/F49/F50/F51 and F54 are bounded runtime/test corrections; deferring them merely to reduce effort is weak justification. They should be independently assessed and, where confirmed, included in this authorized correction iteration.

## Audit conclusion

All six requested Must Fix documentation groups are confirmed at the inspected baselines. Most recommended doc fixes fit the same touched files and should be included. Avoid copying inaccurate review wording (always-failing price, <=0 code, new listenKey rotation, whole-skill authorization, unconditional "never rejected"). A new validation report must cite final branch revisions; this audit is a baseline content review, not verification of subsequent edits.
