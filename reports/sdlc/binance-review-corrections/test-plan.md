# Test-Plan

AC1: scripted quiet stream through 31 minutes, renew failure frame delivery, missing credentials before transport, config repr sentinel checks. AC2: unsupported/malformed metadata rejection, terminal ack cases, account-lock and allowlist ordering, CLI state transitions. AC3: link and skill discovery checks plus Archify validation/browser checks. Functional smoke: real loopback websocket with idle renewal and SQLite persistence, no vendor keys or network. Full suite before each branch commit.
