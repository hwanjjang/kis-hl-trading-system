부분 체결 주문을 거절로 오분류하는 문제와 조건부 주문 목록의 조용한 누락을 오프라인 재현으로 확인했습니다. 관련 테스트는 계정 잠금을 모킹한 상태에서 67개 통과했으며, 1개는 읽기 전용 환경의 임시 디렉터리 제한으로 실행되지 못했습니다.

Full review comments:

- [P1] 종료된 주문의 체결량을 확인한 뒤 rejected로 분류하세요 — /root/.paseo/worktrees/1epgz72k/sulky-dragonfly/kis_hl/binance/trading.py:363-366
  IOC 주문이 일부 체결된 뒤 만료되고 최초 응답이 타임아웃되면, 조회 결과는 `status=EXPIRED`이면서 `executedQty>0`일 수 있습니다. 현재 코드는 체결량과 무관하게 이를 `rejected`로 반환·저장하므로, 실제 포지션이 생겼는데도 실패한 주문으로 판단해 전체 수량을 재주문할 위험이 있습니다. 종료 상태와 체결 여부를 구분하고, 부분 체결된 만료·취소 주문은 미체결 거절로 분류하지 않아야 합니다. 이는 [불확실한 주문 결과의 조회 및 중복 주문 방지 규칙](.agents/skills/binance-api/SKILL.md#L100-L102)에도 관련됩니다.

- [P2] 부분 조회 결과를 계정 전체 주문 목록으로 반환하지 마세요 — /root/.paseo/worktrees/1epgz72k/sulky-dragonfly/kis_hl/cli.py:738-743
  거래소가 심볼 없는 조회에 `-1102`를 반환하면, 일반 주문·포지션·현재 허용 목록에 없는 심볼의 조건부 주문은 조회 대상에서 빠집니다. 예를 들어 DOGEUSDT에 조건부 주문만 남아 있고 허용 목록이 BTCUSDT라면, 실제 주문이 존재해도 `binance-orders`는 빈 목록을 정상 반환합니다. 전체 심볼을 조회하거나, 조회 범위와 불완전 여부를 명시하거나, 명시적인 `--symbol`을 요구해 누락을 숨기지 않아야 합니다.
