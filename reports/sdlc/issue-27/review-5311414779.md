# Assessment of PR review 5311414779

Date: 2026-09-25. Scope: AK requested review-comment assessment only.
Current local and remote PR30 head: `4f27c51525ce243efe564b5756e9074bddc780ca`.
Review: https://github.com/hwanjjang/kis-hl-trading-system/pull/30#pullrequestreview-5311414779

The latest reviewer reports no required findings and confirms resolution of the
previous required finding and two recommendations. It reports 193 passing tests.
This assessment covers its three new recommendations; it does not implement
corrections, publish a reply/body update, or grant merge/live authority.

## Dispositions

1. **Confirmed behavior; prefer explicit evidence-bounded admission.**
   `conditional_add.py:75,79` calls authority validation before and after fresh
   preflight. `strategy_signals.py` in turn calls `validate_add`, which reevaluates
   immutable setup/position evidence and original capital evidence at current time.
   With snapshot max_age_ms=10000, a 9000ms delayed tick submits; at 10500ms the
   tranche is REJECTED despite 29500ms remaining on its plan. The observed reason is
   `Add completed-bar confirmation is unfinished, stale or failed`. Shortening the
   original capital evidence budget instead yields `Account-total evidence is stale
   or invalid`. Both stale cases reject before the gateway preflight runs, although
   the stub is configured to return freshly timestamped total-balance evidence.

   This is an availability/contract clarity issue, not unsafe execution. Issue27
   requires stale confirmation rejection and source revalidation. Fresh account
   balances and quotes alone do not prove the identified completed-bar condition.
   Removing check_authority/validate_add at submission would weaken that contract.
   Prefer the reviewer's second option: reject approval whose expiry exceeds the
   earliest relevant evidence deadline and expose the effective deadline in the
   preview. Include snapshot/position freshness, candle and daily/weekly history
   limits, and original capital evidence capped by max_quote_age_ms, as applicable,
   as well as signal/grant expiry. Preserve submission-time revalidation and fresh
   account/funds reads. Do not silently extend evidence age, shorten approved
   parameters, replace an immutable signal, or introduce a new scheduler.

2. **Confirmed unnecessary permanent rejection for temporary account state.**
   `managed_execution.py:1092-1095` rejects whenever another same-scope/same-mode
   owner is outside FINISHED or PROTECTED. Each of QUEUED, ENTERING, and PROTECTING
   reproduces REJECTED with no add send. Changing the other owner to CLOSED and
   stepping again leaves the tranche REJECTED. A later recovery cannot reuse it.

   Distinguish these temporary reconciliation waits from permanent authority,
   exposure, kill-switch or input failures. Keep the add QUEUED with a local wait
   reason and re-evaluate on existing ticks. This is not a transport failure and
   should not be mislabeled TransientInfoError. Check expiry/revocation before the
   wait branch; otherwise a simple continue would bypass the preflight's authority
   checks and retain expired work indefinitely. On recovery, rerun all freshness,
   capital, exposure and fund checks. Preserve durable signed-attempt non-retry,
   including UNKNOWN, and do not broadly treat every INTERVENTION as transient.

3. **Confirmed stale PR description.**
   GitHub's current body still links the original b6cad43 source/report and 185-test
   result. This omission should be corrected when publication is requested. The
   latest author evidence is 199 passing tests plus smoke at 4f27c51, with independent
   192 tests, final-client 34, and 15 probes. The reviewer's 193 is not contradictory:
   the author's 199-test command includes six websocket tests beyond the listed
   193-test PR scope. These counts describe different suites, not new runs during
   this assessment. Use latest immutable commit/report links and retain offline
   limitations; do not turn COMMENTED/no-required-findings into merge approval.

## Reproduction

```sh
PYTHONPATH=. /tmp/hl-trailing-venv/bin/python reports/sdlc/issue-27/review-5311414779-probe.py
```

Exit 0, six cases captured in `review-5311414779-results.json`. The probe uses
existing fixtures, temporary SQLite and stub gateways, with socket creation
forbidden. Its assertions describe current behavior including the rejected paths;
they are not tests of an implemented fix. No production source, account state,
GitHub body/comment, commit or push was changed. No full-suite rerun was needed
for this assessment. Existing protection remains unchanged.
